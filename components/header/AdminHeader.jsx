import { AppBar } from "@mui/material";

export default function AdminHeader(){
    return (
        <AppBar sx={{textAlign: "center", fontSize:"x-large", padding: 2}}>
            HOTEL BOOKING APP - ADMIN PANEL
        </AppBar>
    )
}